[project]
name = "dify-plugin"
version = "0"
dependencies = [
    "dify-plugin",
]

[dependency-groups]
dev = [
    "pytest"
]

[tool.hatch.build.targets.wheel]
packages = ["tools"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


